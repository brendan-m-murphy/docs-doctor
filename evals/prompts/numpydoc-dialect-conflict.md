Use `$python-docstring-review` to convert the public docstrings in
`evals/fixtures/numpydoc-project/src/wavekit/signal.py` to Google style. You may edit
that file only.

Inspect the fixture project's documentation policy and configured tools first. Do not
change project policy or configuration. If the requested file-level conversion conflicts
with repository policy, report the conflict and the appropriate migration scope instead
of silently creating a mixed dialect. Leave the fixture unchanged in that case.
