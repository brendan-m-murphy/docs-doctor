# Documentation policy

The usage documentation names recommended operational workflows. A public export or
built-in model recipe is supported API, but that alone does not make it the recommended
operational route.

Development documentation defines the architectural requirements for a production
model recipe. Release and usage documentation state whether a workflow has been adopted
for operations.
