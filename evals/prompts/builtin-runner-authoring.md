In a disposable copy of `evals/fixtures/builtin-runner-project`, use
`$documentation-authoring` to document `run_fixed_ou_cached` for scientists who want to
use the built-in runner. You may edit documentation files only. Keep the change bounded,
preserve the documented support policy, and report the resulting file scope and checks.
